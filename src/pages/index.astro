---
import Cards from "../components/cards.astro"
import Header from "../components/header.astro"
import Footer from "../components/footer.astro"
import Layout from "../components/Layout.astro"
//const response = await fetch('https://jsonplaceholder.typicode.com/posts');
//const noticias = await response.json();

---


<Layout titulo=" Cruz Roja Cubana">
	
		<div class="container mx-auto p-4">
			<h1 class="text-3xl font-bold mb-4">Noticias</h1>
			<p class="mb-6">Últimas noticias de la Cruz Roja Cubana</p>
			
			<!-- <div class="flex flex-wrap gap-6">
				{noticias.slice(0, 5).map((noticia: { title: string }) => (
					<Cards titulo={noticia.title} class="flex-1 min-w-[250px]" />
				))}
			</div>
			{noticias.slice(0, 5).map((noticia: { title: string }) => (
				 -->
				
				<!-- <Cards titulo={noticia.title} /> -->
				
			<!-- ))} -->
		
	</div>
</Layout>




<style>
    .noticia {
		border: 1px solid #ccc;
        padding: 16px;
        margin-bottom: 16px; 
        border-radius: 8px;
    }
    .noticia h2 {
        margin-top: 0;
    }